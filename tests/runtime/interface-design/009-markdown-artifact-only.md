---
id: interface-design-009-markdown-artifact-only
title: interface design keeps markdown as the canonical artifact
stage: Interface Design
prompt: Complete the Interface Design stage.
expect:
  - creates docs/interface-design.md or the active Work Item equivalent
  - references optional design files only when they actually exist
  - does not generate an HTML review representation
  - updates Project Memory and waits before Development
tags: [interface-design, artifact, markdown]
---

# Scenario

The user requested the designer stage but explicitly deferred HTML artifact rendering and commenting.
