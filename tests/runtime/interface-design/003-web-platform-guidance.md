---
id: interface-design-003-web-platform-guidance
title: web interface design uses web and accepted stack guidance
stage: Interface Design
prompt: Design the accepted web interface for implementation.
expect:
  - loads only web platform guidance and relevant accepted stack rules
  - specifies responsive keyboard focus URL navigation loading error and empty behavior when relevant
  - preserves semantic platform behavior before custom interaction
  - keeps visual decisions independent from framework fashion
tags: [interface-design, web, progressive-loading]
---

# Scenario

Architecture accepted a React and shadcn/ui web application.

The interface contains navigation, forms, asynchronous data, and responsive layouts.
