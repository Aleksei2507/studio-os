---
id: architecture-015-unknown-model-evidence
title: an unknown model identity does not become a fabricated catalog
stage: Architecture
prompt: Prepare an execution plan using what this host actually supports.
expect:
  - distinguishes confirmed native delegation from unavailable model selection
  - records unreported model identities token usage and pricing as Unknown
  - uses same-model-delegation only through the confirmed inherited or default host mechanism or selects justified single-model execution
  - does not infer model capabilities prices or available identities from remembered brand names
  - explains the overhead and evidence required before accepting a delegated result
  - does not promise a model switch or dispatch development during Architecture
tags: [architecture, model-orchestration, capability]
---

# Scenario

The accepted architecture contains several bounded tasks. This host exposes a native child-agent tool that inherits its configuration, but exposes neither a per-child model selector nor a model catalog. The current model identity, token usage and monetary cost are not reported. No external provider or new tool installation has been requested.
