---
id: design-strategy-002-brownfield-preserves-patterns
title: brownfield design strategy preserves stable conventions
stage: Design Strategy
prompt: Add a new flow to an existing product with established interaction patterns.
expect:
  - reads the observed Project Design System Profile before recommending visual direction
  - inspects and preserves stable existing design conventions
  - changes the interaction model only when the accepted feature requires it
  - records constraints for Planning and Architecture
tags: [design-strategy, brownfield, consistency]
---

# Scenario

The product already has an Observed Project Design System Profile plus accepted navigation, form, feedback, and accessibility patterns.

Design Strategy should integrate the new flow instead of redesigning the product because another visual direction appears newer.
