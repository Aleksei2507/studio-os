# Support Queue Board

Support Queue Board is an internal browser application for triaging customer
support requests. It shows open requests, their priority, and the currently
assigned support specialist.

The current product is a small React application. Shared interface primitives
live in `src/ui`, and visual tokens live in `src/styles/tokens.css`.

## Quality Commands

- `npm test`
- `npm run typecheck`
